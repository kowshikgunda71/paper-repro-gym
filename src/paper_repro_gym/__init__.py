"""paper-repro-gym — a gated, containerized reproduction workbench.

Reproduction (re-running the authors' own artifacts), not replication.
Containment, not a sandbox. Nothing runs without a signed A1 approval.
"""
__version__ = "0.1.0"
